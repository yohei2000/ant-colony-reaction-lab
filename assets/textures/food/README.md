Food Texture Overrides

The game currently uses procedural CanvasTexture fallbacks defined by
`FOOD_TEXTURE_MANIFEST` in `src/simulation.js`.

Drop replacement PNGs here using the manifest filenames when art production is
ready. Keep them top-down, square, color-space sRGB, and small enough for mobile
use, preferably 256x256 or 512x512.
